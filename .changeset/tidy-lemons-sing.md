---
"cricket": patch
---

Automate version tag generation on commit into master with a version bump(version in package.json doesn't have a tag yet). Also only run changesets if this is not a push of a fresh version bump.
